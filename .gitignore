# 依赖与构建
node_modules/
.next/
out/
*.tsbuildinfo

# 环境变量
.env
.env.local
.env.*.local

# 采集的原始大数据（不入库，可重新采集）
scripts/data/store-products.json
scripts/data/store-matched.json
scripts/data/siglent-raw.json

# 上传的媒体（图片体积大，不入库；从线上可恢复）
public/uploads/

# 系统文件
.DS_Store
Thumbs.db
dev-server.log
